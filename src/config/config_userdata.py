
from src.config.config import config

userdata_config = {

    'userdata_filename': 'data/channel_of_' + config['channel'] + '/viewerslist.json',
    'updata_interval': 5 # in minutes, 0 to disable

} # End of userdata_config
